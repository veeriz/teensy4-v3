[env:teensy40]
platform = teensy
board = teensy40
framework = arduino
lib_deps =
    tonton81/FlexCAN_T4@^2.0.8

; Build flags including include paths
build_flags = 
    -D USB_SERIAL
    -D TEENSY_OPT_FASTEST
    -I include
    -I src

; Rest of the configuration remains the same...